version: '3.8'

services:
  cors-proxy:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: logii-cors-proxy
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - proxy-network

networks:
  proxy-network:
    driver: bridge
